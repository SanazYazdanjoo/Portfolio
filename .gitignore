node_modules
dist
.DS_Store

# Local secrets (OPENAI_API_KEY for the chat endpoint) — see .env.example
.env

# Build-generated delivery images; source photography stays tracked.
src/projects/smart-home-control/media/.optimized/

# Personal content worklist generated by the Evidence Layer build — not
# documentation, not for the repo. See scripts/check-needs-input.mjs.
CONTENT_GAPS.md