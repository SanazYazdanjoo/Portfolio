# assets — designing-this-site

Placeholder. No real screenshots, wireframes, or moodboard images exist yet for
this case study, so `data.js` ships without a `thumbnail`, `heroImage`, or any
`figures` entries — per the "no invented data" rule, nothing here is a stand-in
image pretending to be evidence.

When real assets are available, add them here and wire them into `data.js`:

- a hero screenshot of the homepage (`thumbnail` / `thumbnailWebp`)
- process-gallery screenshots for the `define`/`design` phase entries
  (`process[].imagePath`)
- any figures for `challenge` / `solution` / `prototype`
  (`figures.challenge` / `figures.solution` / `figures.prototype`)
